import React, {PropTypes} from 'react';

export const SearchTitle = (props) => {
  return (
    <div className="topbanner">
      <h2>Результати пошуку</h2>
    </div>
  );
}

SearchTitle.propTypes = {
};
